// use function to runs and outputs "Hello" to the terminal, function can not run on its own.
#include <iostream>

using namespace std;

// Function that prints "Hello"
void hello() {
  cout << "Hello" << endl;
}
// functions are use to write a program once and use it again and again, it’s a user defined, programmer creates this.
int main() { // functions prevent repeated action of writing code again and again.
  hello(); // Call the function
  return 0;
}
